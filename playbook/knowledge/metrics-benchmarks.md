# Knowledge Base: Metrics & Benchmarks

**What belongs here:** the numbers themselves — reply rate/positive reply rate benchmarks, open
rate context, what "good" looks like by stage — a reference doc other KB files can point to
instead of re-quoting a benchmark inline every time. 96 tagged posts in the corpus under
`domain_primary: metrics-benchmarks` — the thinnest of the 10 KB domains, likely a single file for
a long time (and likely to overlap in citation with `deliverability-infra.md` and
`copywriting.md`, which already carry some benchmark numbers directly relevant to their own
sections — cross-link rather than duplicate once this fills in).

## Sources ingested
- **Corpus:** not yet extracted. Run `node scripts/extract-domain-synthesis-source.mjs --domain
  metrics-benchmarks --top 50` for the source dump when writing the real synthesis pass.
- **Received lead magnets:** none remapped to this domain yet (see
  `scripts/remap-leadmagnet-domains.mjs` for the full remapping pass, 2026-07-10).
