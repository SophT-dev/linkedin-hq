# Knowledge Base: Channel Strategy

**What belongs here:** which channels to run and how to sequence/combine them (email vs.
LinkedIn vs. call vs. multichannel), channel-mix decisions — the "where do we show up" layer above
any single channel's own tactics. 123 tagged posts in the corpus under `domain_primary:
channel-strategy` — the second-thinnest of the 10 KB domains, likely a single file for a long
time.

## Sources ingested
- **Corpus:** not yet extracted. Run `node scripts/extract-domain-synthesis-source.mjs --domain
  channel-strategy --top 50` for the source dump when writing the real synthesis pass.
- **Received lead magnets:** none remapped to this domain yet (see
  `scripts/remap-leadmagnet-domains.mjs` for the full remapping pass, 2026-07-10).
